//MODULE
//commonJS, every files is module (by default)
//mudules -encapsulated code (only shared minimum)
const secret="secret"
const sam="sam"
const peter="peter"
const a=(somename)=>{
    console.log(`hi ${somename}`)
}

